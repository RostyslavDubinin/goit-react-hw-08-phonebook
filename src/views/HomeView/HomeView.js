import styles from "./HomeView.module.css";

const HomeView = () => (
  <div className={styles.wrapper}>
    <h1 className={styles.heading}>Hi there! Please, sign in to continue</h1>
  </div>
);

export default HomeView;
